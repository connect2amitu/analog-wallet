{
	"name": "analog",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"private": true,
	"workspaces": [
		"packages/**"
	],
	"scripts": {
		"start": "yarn workspace extension start",
		"build": "yarn workspace extension build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/chrome": "^0.0.199"
	}
}
