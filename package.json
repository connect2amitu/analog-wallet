{
	"name": "analog",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"private": true,
	"workspaces": [
		"packages/**"
	],
	"scripts": {
    "build:ui": "cd packages/extension && yarn webpack --config webpack.extension.cjs --mode production",
    "watch": "cd packages/extension && yarn webpack --config webpack.watch.cjs --mode development --watch"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/chrome": "^0.0.199"
	}
}
